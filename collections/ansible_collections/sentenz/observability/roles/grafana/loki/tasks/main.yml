# SPDX-License-Identifier: Apache-2.0
---
- name: Loki - Ensure group ownership
  ansible.builtin.group:
    name: "{{ grafana_ownership_group }}"
    state: present
    system: true

- name: Loki - Ensure user ownership
  ansible.builtin.user:
    name: "{{ grafana_ownership_owner }}"
    group: "{{ grafana_ownership_group }}"
    state: present

- name: Loki - Securely create directories
  ansible.builtin.file:
    state: directory
    path: "{{ grafana_loki_path }}"
    owner: "{{ grafana_ownership_owner }}"
    group: "{{ grafana_ownership_group }}"
    mode: "{{ grafana_permission_directory }}"

- name: Loki - Securely copy content
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ grafana_loki_path }}/{{ item }}"
    owner: "{{ grafana_ownership_owner }}"
    group: "{{ grafana_ownership_group }}"
    mode: "{{ grafana_permission_file }}"
  loop:
    - config.yml

- name: Loki - Securely template Jinja2 files
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ grafana_loki_path }}/docker-compose.yml"
    owner: "{{ grafana_ownership_owner }}"
    group: "{{ grafana_ownership_group }}"
    mode: "{{ grafana_permission_file }}"
  notify:
    - grafana_restart

- name: Loki - Deploy Containers
  community.docker.docker_compose_v2:
    project_src: "{{ grafana_loki_path }}"
    state: present

- name: Loki - Destroy Containers
  community.docker.docker_compose_v2:
    project_src: "{{ grafana_loki_path }}"
    files:
      - docker-compose.yml
    state: absent
  tags:
    - destroy
  notify:
    - grafana_absent

- name: Loki - Restart Containers
  community.docker.docker_compose_v2:
    project_src: "{{ grafana_loki_path }}"
    files:
      - docker-compose.yml
    state: restarted
  tags:
    - restart
  notify:
    - grafana_restart

- name: Loki - Stop Containers
  community.docker.docker_compose_v2:
    project_src: "{{ grafana_loki_path }}"
    files:
      - docker-compose.yml
    state: stopped
  tags:
    - stop
  notify:
    - grafana_stop
