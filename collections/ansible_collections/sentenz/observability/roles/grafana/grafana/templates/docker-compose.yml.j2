---
services:
  {{ grafana_name }}:
    image: {{ grafana_image }}
    container_name: {{ grafana_name }}
    user: {{ grafana_ownership_owner }}
    volumes:
      # - {{ grafana_path }}/grafana.ini:/etc/grafana:ro
      - {{ grafana_path }}/dashboards:/var/lib/grafana/dashboards:ro
      - {{ grafana_path }}/provisioning:/etc/grafana/provisioning:ro
      - grafana_data:/var/lib/grafana
    ports:
      - "{{ grafana_port }}:3000"
    restart: always
    networks:
      - observability
    deploy:
      mode: global
    security_opt:
      - no-new-privileges:true

networks:
  observability:
    name: observability
    driver: bridge

volumes:
  grafana_data: {}
