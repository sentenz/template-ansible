# SPDX-License-Identifier: Apache-2.0
---
- name: Container Docker - {{ ansible_distribution }} - Remove unofficial packages
  ansible.builtin.apt:
    name: "{{ item }}"
    state: absent
    purge: true
  loop:
    - docker.io
    - docker-compose
    - docker-compose-v2
    - docker-doc
    - podman-docker
    - containerd
    - runc
  ignore_errors: true

- name: Container Docker - {{ ansible_distribution }} - Update packages
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Container Docker - {{ ansible_distribution }} - Setup packages
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop:
    - apt-transport-https
    - ca-certificates
    - curl
    - gnupg
    - lsb-release

- name: Container Docker - {{ ansible_distribution }} - Add GPG key
  ansible.builtin.apt_key:
    url: https://download.docker.com/linux/ubuntu/gpg
    state: present

- name: Container Docker - {{ ansible_distribution }} - Add repository
  ansible.builtin.apt_repository:
    repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
    state: present
    filename: docker

- name: Container Docker - {{ ansible_distribution }} - Update packages
  ansible.builtin.apt:
    update_cache: true

- name: Container Docker - {{ ansible_distribution }} - Install packages
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop:
    - docker-ce
    - docker-ce-cli
    - containerd.io
    - docker-buildx-plugin
    - docker-compose-plugin
  notify:
    - docker_start

- name: Container Docker - {{ ansible_distribution }} - Add user group
  ansible.builtin.user:
    name: "{{ item }}"
    groups: "{{ docker_ownership_owner }}"
    append: true
  loop: "{{ docker_users }}"
  when: docker_users | length > 0
  notify:
    - docker_restart
