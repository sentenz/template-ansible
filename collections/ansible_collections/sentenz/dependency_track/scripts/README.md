# `/scripts`

> NOTE Perform the command line `apt-cache madison <package> | awk '{ print $3 }'` to print the `apt` package version.

1. Files and Folders

    - `setup.sh`
      > TODO

    - `setup_analysis.sh`
      > TODO

    - `setup_release.sh`
      > TODO

    - `setup_testing.sh`
      > TODO

    - `setup_xac.sh`
      > TODO

    - `/shell`
      > TODO
